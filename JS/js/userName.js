function UserName({ id, email, first_name, last_name, avatar }) {
    this.Id = id;
    this.Email = email;
    this.Firt_name = first_name;
    this.Last_name = last_name;
    this.Avatar = avatar
     return this.Id, this.Email, this.Firt_name, this.Last_name, this.Avatar
}

export default UserName